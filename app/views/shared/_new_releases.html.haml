%h2= t('brand_new')
.dashboard-unit-inner
  %ul.unstyled.sub-unsub
    - releases.each do |r|
      %li
        = r.decorate.full_title
        &nbsp;
        %button.btn.btn-success.btn-mini.fade{ data: { action: 'Subscribe', instance_id: r.id, 'original-title' => 'Subscribe'}}
          %i.icon-plus
