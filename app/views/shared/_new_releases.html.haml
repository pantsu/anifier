%h2 Brand new
= form_tag "#", class: "dashboard-unit-inner" do
  - releases.each do |r|
    = label_tag :release, class: "checkbox" do
      = check_box_tag :release
      = r.decorate.full_title

  = submit_tag "Subscribe", class: "btn btn-inverse dashboard-submit"